#!/bin/bash
# Git pre-commit hook
# Note: sync_llm_containers.py is no longer needed - both containers now use shared/llm_base.py

# This hook is kept for future use or can be removed if not needed

exit 0

