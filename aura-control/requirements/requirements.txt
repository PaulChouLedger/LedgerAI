# Core Python Dependencies for LedgerAI Aura Control
# Install with: pip install -r requirements.txt

# Audio Processing
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.10.0

# Wake Word Detection: OpenWakeWord (Actively maintained, works on all platforms)
# Install: pip install openwakeword
# Models are downloaded automatically on first use
# Note: Requires numpy, pandas, and scikit-learn (must be pip-installed, not system packages)
# IMPORTANT: Use Jetson-optimized onnxruntime-gpu from Jetson AI Lab PyPI
# The install script will install onnxruntime-gpu from https://pypi.jetson-ai-lab.io/jp6/cu126
# This is specifically built for Jetson devices and avoids ARM CPU detection issues
# OpenWakeWord is compatible with onnxruntime-gpu-1.23.0 and will use it if installed first
openwakeword>=0.5.0
scikit-learn>=1.3.0
pandas>=2.0.0

# GUI Framework
PyQt5>=5.15.0

# HTTP and Networking
requests>=2.31.0
urllib3>=1.26.0

# Environment Configuration
python-dotenv>=1.0.0

# Text-to-Speech
elevenlabs>=0.2.0

# USB Device Access (for ReSpeaker)
pyusb>=1.2.1

# Web Server (for upload functionality)
flask>=2.3.3
werkzeug>=2.3.7

# Google Drive Integration (optional)
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Telegram Bot (optional)
python-telegram-bot>=20.0

# Ethereum & Web3 Integration (for wallet connectivity)
web3>=6.11.0
eth-account>=0.10.0
eth-utils>=2.3.0

# QR Code generation for MetaMask
qrcode[pil]>=7.4.0

