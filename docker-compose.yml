version: "3"
services:
  whisper:
    build: ./whisper-container
    network_mode: host
    volumes:
      - ./shared:/shared

  llm:
    image: aura-llm-rag
    network_mode: host
    volumes:
      - ./shared:/shared
      - ./data:/app/data  # Mount embeddings data

